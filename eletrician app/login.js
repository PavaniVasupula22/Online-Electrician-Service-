// Additional logic for handling login if needed
document.getElementById('loginForm').addEventListener('submit', function(event) {
    event.preventDefault();
    // Handle login-specific logic if needed
});
